stages:
  - build
  - test

build-image:
  stage: build
  tags:
    - docker
  script:
    - docker build -t dq-discovery .


run-image:
  stage: test
  tags:
    - docker
  script:
    - docker run --rm dq-discovery echo "Container ran successfully!"

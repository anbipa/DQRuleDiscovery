stages:
  - build
  - test

build-image:
  stage: build
  tags:
    - ubuntu
  script:
    - docker build -t dq-discovery .

run-image:
  stage: test
  tags:
    - ubuntu
  script:
    - docker run --rm dq-discovery echo "Container ran successfully!"
